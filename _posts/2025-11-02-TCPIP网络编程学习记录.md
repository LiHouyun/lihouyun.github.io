---
title: 《TCP/IP 网络编程》学习记录
data: 2025-11-02 21:10:00 +0800
category: 学习记录
tag: [网络, TCP, IP]
description: 简要记录我的《TCP/IP 网络编程》学习过程。
image: 
---

## Chapter 1

### Section 1.1
![](/assets/img-md/19-TCPIP网络编程/1.1-1-run-hello_server.png)
_run hello\_server.c_
![](/assets/img-md/19-TCPIP网络编程/1.1-2-run-hello_client.png)
_run hello\_client.c_

### Section 1.2
![](/assets/img-md/19-TCPIP网络编程/1.2-1-run-low_open.png)
_run low\_open.c_
![](/assets/img-md/19-TCPIP网络编程/1.2-2-run-low_read.png)
_run low\_read.c_
![](/assets/img-md/19-TCPIP网络编程/1.2-3-run-fd_seri.png)
_run fd\_seri.c_

## Chapter 2

## Section 2.1 
![](/assets/img-md/19-TCPIP网络编程/2.1-1-run-tcp_server.png)
_run tcp\_server.c_
![](/assets/img-md/19-TCPIP网络编程/2.1-2-run_tcp_client.png)
_run tcp\_client.c_

## Error
### bind() error
#### 问题描述
短时间内连续两次运行 `./hserver 9190` 会报错 `bind() error`
![](/assets/img-md/19-TCPIP网络编程/2.1-3-error-bind.png)
_bind() error_

#### 问题原因
之前运行过服务器，TCP 端口需要一段时间才能完全释放。等待1-2分钟再重试既可。这是典型的 **TCP 端口 TIME_WAIT 状态** 问题。

当关闭服务器程序后，TCP 套接字会进入 TIME_WAIT 状态，通常持续 1-2 分钟。在这个状态下：

- 操作系统保留端口号，确保网络中所有数据包都已完成传输
- 相同的 IP:端口组合暂时无法立即重用

#### 解决方案

##### 方法1：设置 SO_REUSEADDR 套接字选项

在 `bind()` 之前添加：

```c
// 在 socket() 之后，bind() 之前添加：
int optval = 1;
setsockopt(serv_sock, SOL_SOCKET, SO_REUSEADDR, &optval, sizeof(optval));
```

完整代码示例：
```c
serv_sock = socket(PF_INET, SOCK_STREAM, 0);
if (serv_sock == -1)
    error_handling("socket() error");

// 添加这3行：允许地址重用
int optval = 1;
setsockopt(serv_sock, SOL_SOCKET, SO_REUSEADDR, &optval, sizeof(optval));

memset(&serv_addr, 0, sizeof(serv_addr));
// ... 其余代码不变
```

##### 方法2：等待 TIME_WAIT 超时
就像你发现的，等待 1-2 分钟端口自动释放。

##### 方法3：使用不同的端口号
```bash
./hserver 9191
./hserver 9192
```
